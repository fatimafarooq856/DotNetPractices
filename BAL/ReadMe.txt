 If you don't the warning on fields for nonn-nullable value, you can disable this by deleting the below line from the csproj file
 or setting it as disable. By default value is disable.

<Nullable>enable</Nullable>